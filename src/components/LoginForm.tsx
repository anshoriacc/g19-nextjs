import React from 'react';
import { Form } from 'antd';

export default function LoginForm() {
  return (
    <section className="absolute w-full h-full flex justify-center items-center z-30">
      <Form>asd</Form>
    </section>
  );
}
